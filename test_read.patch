--- pkg/event/decoder.go
+++ pkg/event/decoder.go
@@ -122,6 +122,7 @@
 		if truncated {
 			actualLen = printLimit + 1
 		}
+		if strings.Contains(string(raw), "///") { fmt.Printf("DEBUG: truncated=%v actualLen=%d printLimit=%d limit=%d len(raw)=%d\n", truncated, actualLen, printLimit, limit, len(raw)) }
 		finalRes = format.BufferEscape(raw, printLimit, actualLen, d.HexEscapeMode)
 	} else {
 		// String decoding failed, fallback to treating it as a pointer
