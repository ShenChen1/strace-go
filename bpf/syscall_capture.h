/* Generated by generate-syscalls. DO NOT EDIT. */
#ifndef __SYSCALL_CAPTURE_H__
#define __SYSCALL_CAPTURE_H__

#define CAPTURE_ARGS_ENTER(sys_id, e) \
	switch (sys_id) { \
		case 1: /* write */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] > 512 ? 512 : (e)->args[2]) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] > 512 ? 512 : (e)->args[2]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 2: /* open */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 4: /* stat */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 6: /* lstat */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 7: /* poll */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0), (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 13: /* rt_sigaction */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 32, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 14: /* rt_sigprocmask */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 8, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 16: /* ioctl */ \
			(e)->ptr = (e)->args[2]; \
			{ \
				u32 iosz = (((e)->args[1] >> 16) & 0x3fff); \
				iosz = (iosz == 0) ? 128 : (iosz > 512 ? 512 : iosz); \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 512, iosz, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? iosz : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 18: /* pwrite64 */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] > 512 ? 512 : (e)->args[2]) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] > 512 ? 512 : (e)->args[2]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 19: /* readv */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 20: /* writev */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 21: /* access */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 23: /* select */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 128, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 128, 128, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 128 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 256, 128, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 256 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg + 384, 16, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 384 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 35: /* nanosleep */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 38: /* setitimer */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 32, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 40: /* sendfile */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 512, 8, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 42: /* connect */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 128, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 43: /* accept */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 44: /* sendto */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 512, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 512 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg + 512, 128, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 45: /* recvfrom */ \
			{ \
				long __err = (e)->args[5] ? bpf_probe_read_user((e)->str_arg + 768, 4, (void *)(e)->args[5]) : 0; \
				long pr = (__err == 0 && (e)->args[5]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 5)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 49: /* bind */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 128, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 51: /* getsockname */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 52: /* getpeername */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 59: /* execve */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 72: /* fcntl */ \
			{ \
				u32 fcmd = (u32)(e)->args[1]; \
				u32 fsz = 0; \
				if (fcmd == 15 || fcmd == 16 || fcmd == 1035 || fcmd == 1036 || fcmd == 1037 || fcmd == 1038 || fcmd == 1039 || fcmd == 1040 || fcmd == 1043 || fcmd == 1044 || fcmd == 19 || fcmd == 20 || fcmd == 21 || fcmd == 22 || fcmd == 23 || fcmd == 24) fsz = 8; \
				else if (fcmd == 5 || fcmd == 6 || fcmd == 7 || fcmd == 12 || fcmd == 13 || fcmd == 14 || fcmd == 36 || fcmd == 37 || fcmd == 38) fsz = 32; \
				long __err = (fsz > 0 && (e)->args[2]) ? bpf_probe_read_user((e)->str_arg, fsz, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && fsz > 0 && (e)->args[2]) ? fsz : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 76: /* truncate */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 80: /* chdir */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 82: /* rename */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 83: /* mkdir */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 84: /* rmdir */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 85: /* creat */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 86: /* link */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 87: /* unlink */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 88: /* symlink */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 89: /* readlink */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 90: /* chmod */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 92: /* chown */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 94: /* lchown */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 125: /* capget */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 8, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 126: /* capset */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 8, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 130: /* rt_sigsuspend */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 8, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 132: /* utime */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 512, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 133: /* mknod */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 137: /* statfs */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 157: /* prctl */ \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 16, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 158: /* arch_prctl */ \
			(e)->ptr = (e)->args[1]; \
			break; \
		case 159: /* adjtimex */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 208, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 208 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 160: /* setrlimit */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 161: /* chroot */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 163: /* acct */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 164: /* settimeofday */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 16, 8, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 16 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 165: /* mount */ \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[2] ? bpf_probe_read_user_str((e)->str_arg + 1024, 128, (void *)(e)->args[2]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[4] ? bpf_probe_read_user_str((e)->str_arg + 1152, 512, (void *)(e)->args[4]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 1152 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 166: /* umount2 */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 167: /* swapon */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 168: /* swapoff */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 188: /* setxattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 189: /* lsetxattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 190: /* fsetxattr */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 256, ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 256 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 191: /* getxattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 192: /* lgetxattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 193: /* fgetxattr */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 194: /* listxattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 195: /* llistxattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 197: /* removexattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 198: /* lremovexattr */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 512, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 199: /* fremovexattr */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 256, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 202: /* futex */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 208: /* io_getevents */ \
			{ \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg + 512, 16, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[5] ? bpf_probe_read_user((e)->str_arg + 528, 16, (void *)(e)->args[5]) : 0; \
				long pr = (__err == 0 && (e)->args[5]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 5)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 528 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				void *__ptr = NULL; \
				long __err1 = (e)->args[5] ? bpf_probe_read_user(&__ptr, sizeof(void*), (void *)(e)->args[5]) : 0; \
				long __err = (__err1 == 0 && __ptr) ? bpf_probe_read_user((e)->str_arg + 544, 8, __ptr) : __err1; \
				long pr = (__err == 0 && __ptr) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 13)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 544 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 209: /* io_submit */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg, ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			for (int i = 0; i < 2; i++) { \
				u64 p; \
				if (bpf_probe_read_user(&p, 8, (void *)(e->args[2] + i*8)) == 0 && p != 0) { \
					bpf_probe_read_user((e)->str_arg + 512 + i*64, 64, (void *)p); \
					if ((e)->data_len < 512 + i*64 + 64) (e)->data_len = 512 + i*64 + 64; \
				} \
			} \
			break; \
		case 210: /* io_cancel */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 64, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 64 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 227: /* clock_settime */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 230: /* clock_nanosleep */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 233: /* epoll_ctl */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg, 12, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 12 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 235: /* utimes */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 512, 32, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 248: /* add_key */ \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 64, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 64, 128, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 64 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 256, ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 256 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 249: /* request_key */ \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 64, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg + 64, 128, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 64 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[2] ? bpf_probe_read_user_str((e)->str_arg + 256, 256, (void *)(e)->args[2]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 256 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 257: /* openat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 258: /* mkdirat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 259: /* mknodat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 260: /* fchownat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 261: /* futimesat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 512, 32, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 262: /* newfstatat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 263: /* unlinkat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 264: /* renameat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[3] ? bpf_probe_read_user_str((e)->str_arg + 512, 512, (void *)(e)->args[3]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 265: /* linkat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 266: /* symlinkat */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = (e)->args[0] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[0]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[2] ? bpf_probe_read_user_str((e)->str_arg + 512, 512, (void *)(e)->args[2]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 267: /* readlinkat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 268: /* fchmodat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 269: /* faccessat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 271: /* ppoll */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0), (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 512, 16, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 278: /* vmsplice */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 280: /* utimensat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 512, 32, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 288: /* accept4 */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 295: /* preadv */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 296: /* pwritev */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 302: /* prlimit64 */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 305: /* clock_adjtime */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, 208, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 208 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 310: /* process_vm_readv */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 512, ((e)->args[4] > 0 ? ((e)->args[4] * 16 > 512 ? 512 : (e)->args[4] * 16) : 0), (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? ((e)->args[4] > 0 ? ((e)->args[4] * 16 > 512 ? 512 : (e)->args[4] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 311: /* process_vm_writev */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 512, ((e)->args[4] > 0 ? ((e)->args[4] * 16 > 512 ? 512 : (e)->args[4] * 16) : 0), (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? ((e)->args[4] > 0 ? ((e)->args[4] * 16 > 512 ? 512 : (e)->args[4] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 316: /* renameat2 */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = (e)->args[1] ? bpf_probe_read_user_str((e)->str_arg, 512, (void *)(e)->args[1]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long pr = (e)->args[3] ? bpf_probe_read_user_str((e)->str_arg + 512, 512, (void *)(e)->args[3]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 319: /* memfd_create */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 250, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 250 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 321: /* bpf */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] > 512 ? 512 : (e)->args[2]) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] > 512 ? 512 : (e)->args[2]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 322: /* execveat */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 326: /* copy_file_range */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 512, 8, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 520, 8, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 520 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 327: /* preadv2 */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 328: /* pwritev2 */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 333: /* io_pgetevents */ \
			{ \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg + 512, 16, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[5] ? bpf_probe_read_user((e)->str_arg + 528, 16, (void *)(e)->args[5]) : 0; \
				long pr = (__err == 0 && (e)->args[5]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 5)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 528 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				void *__ptr = NULL; \
				long __err1 = (e)->args[5] ? bpf_probe_read_user(&__ptr, sizeof(void*), (void *)(e)->args[5]) : 0; \
				long __err = (__err1 == 0 && __ptr) ? bpf_probe_read_user((e)->str_arg + 544, 8, __ptr) : __err1; \
				long pr = (__err == 0 && __ptr) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 13)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 544 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 430: /* fsopen */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long pr = 0; \
				if ((e)->args[0]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[0]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[0] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[0] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 431: /* fsconfig */ \
			(e)->ptr = (e)->args[2]; \
			{ \
				long pr = (e)->args[2] ? bpf_probe_read_user_str((e)->str_arg, 257, (void *)(e)->args[2]) : 0; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				u32 switchsz = 0; \
				if ((e)->args[1] == 2) { \
					switchsz = (e)->args[4]; \
					switchsz &= 0x1fff; \
					switchsz = (switchsz > 4096) ? 4096 : switchsz; \
				} \
				long pr = 0; \
				if ((e)->args[1] == 2) { \
					if (switchsz > 0 && (e)->args[3]) { \
						int __err = bpf_probe_read_user((e)->str_arg + 257, switchsz, (void *)(e)->args[3]); \
						pr = (__err == 0) ? switchsz : __err; \
					} \
				} else if ((e)->args[3]) { \
					pr = bpf_probe_read_user_str((e)->str_arg + 257, 4096, (void *)(e)->args[3]); \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 257 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 433: /* fspick */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 435: /* clone3 */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, ((e)->args[1] > 0 ? ((e)->args[1] > 256 ? 256 : (e)->args[1]) : 0), (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? ((e)->args[1] > 0 ? ((e)->args[1] > 256 ? 256 : (e)->args[1]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 437: /* openat2 */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 4096, ((e)->args[3] >= 24 ? ((e)->args[3] > 64 ? 64 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] >= 24 ? ((e)->args[3] > 64 ? 64 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 4096 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 439: /* faccessat2 */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long pr = 0; \
				if ((e)->args[1]) { \
					((e)->str_arg)[0] = 0; \
					pr = bpf_probe_read_user_str((e)->str_arg, 2048, (void *)(e)->args[1]); \
					if (pr < 0) { ((e)->str_arg)[0] = 0; } \
					else if (pr >= 2048) { \
						((e)->str_arg + 2047)[0] = 0; \
						long pr2 = bpf_probe_read_user_str((e)->str_arg + 2047, 2049, (void *)((e)->args[1] + 2047)); \
						if (pr2 >= 0) { \
							pr = 2047 + pr2; \
							if (pr == 4096) { \
								char last_byte = 0; \
								bpf_probe_read_user(&last_byte, 1, (void *)((e)->args[1] + 4095)); \
								((e)->str_arg + 4095)[0] = last_byte; \
							} \
						} else { pr = pr2; ((e)->str_arg + 2047)[0] = 0; } \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 440: /* process_madvise */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] * 16 > 512 ? 512 : (e)->args[2] * 16) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 441: /* epoll_pwait2 */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 512, 16, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 449: /* futex_waitv */ \
			{ \
				u32 count = (u32)(e)->args[1]; \
				if (count > 128) count = 128; \
				u32 countsz = count * 24; \
				long pr = 0; \
				if (countsz > 0 && (e)->args[0]) { \
					long __err = bpf_probe_read_user((e)->str_arg, 24, (void *)(e)->args[0]); \
					if (__err < 0) { \
						pr = __err; \
					} else { \
						pr = 24; \
						u32 countsz_rest = 0; \
						if (countsz > 24) countsz_rest = countsz - 24; \
						if (countsz_rest > 0) { \
							long __err2 = bpf_probe_read_user((e)->str_arg + 24, countsz_rest, (void *)((e)->args[0] + 24)); \
							if (__err2 == 0) pr = countsz; \
						} \
					} \
				} \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 3072, 16, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 3072 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 451: /* cachestat */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 512, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 455: /* futex_wait */ \
			{ \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg, 16, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 456: /* futex_requeue */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg, 48, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 48 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_enter; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_enter = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_enter == -1) (e)->probe_ret_enter = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
	}
#define CAPTURE_ARGS_EXIT(sys_id, e) \
	switch (sys_id) { \
		case 0: /* read */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 4: /* stat */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 144, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 144 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 5: /* fstat */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 144, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 144 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 6: /* lstat */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 144, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 144 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 7: /* poll */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0), (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 13: /* rt_sigaction */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, 32, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 14: /* rt_sigprocmask */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 16: /* ioctl */ \
			{ \
				u32 iosz = (((e)->args[1] >> 16) & 0x3fff); \
				iosz = (iosz == 0) ? 128 : (iosz > 512 ? 512 : iosz); \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, iosz, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? iosz : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 17: /* pread64 */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 22: /* pipe */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 23: /* select */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 128, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1152, 128, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1152 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 1280, 128, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1280 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg + 1408, 16, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1408 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 35: /* nanosleep */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 36: /* getitimer */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 32, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 38: /* setitimer */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, 32, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 32 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 40: /* sendfile */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 43: /* accept */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				u32 addrlen = 0; \
				bpf_probe_read_user(&addrlen, 4, (void *)(e)->args[2]); \
				u32 inlen = *(u32 *)((e)->str_arg + 768); \
				if (inlen > 0 && inlen < addrlen) addrlen = inlen; \
				addrlen = (addrlen > 128) ? 128 : addrlen; \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, addrlen, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? addrlen : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 772, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 772 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 45: /* recvfrom */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 512, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 512 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				u32 addrlen = 0; \
				bpf_probe_read_user(&addrlen, 4, (void *)(e)->args[5]); \
				u32 inlen = *(u32 *)((e)->str_arg + 768); \
				if (inlen > 0 && inlen < addrlen) addrlen = inlen; \
				addrlen = (addrlen > 128) ? 128 : addrlen; \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg + 1536, addrlen, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? addrlen : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1536 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[5] ? bpf_probe_read_user((e)->str_arg + 772, 4, (void *)(e)->args[5]) : 0; \
				long pr = (__err == 0 && (e)->args[5]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 5)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 772 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 51: /* getsockname */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				u32 addrlen = 0; \
				bpf_probe_read_user(&addrlen, 4, (void *)(e)->args[2]); \
				u32 inlen = *(u32 *)((e)->str_arg + 768); \
				if (inlen > 0 && inlen < addrlen) addrlen = inlen; \
				addrlen = (addrlen > 128) ? 128 : addrlen; \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, addrlen, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? addrlen : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 772, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 772 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 52: /* getpeername */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				u32 addrlen = 0; \
				bpf_probe_read_user(&addrlen, 4, (void *)(e)->args[2]); \
				u32 inlen = *(u32 *)((e)->str_arg + 768); \
				if (inlen > 0 && inlen < addrlen) addrlen = inlen; \
				addrlen = (addrlen > 128) ? 128 : addrlen; \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, addrlen, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? addrlen : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 772, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 772 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 53: /* socketpair */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 63: /* uname */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, 390, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 390 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 72: /* fcntl */ \
			{ \
				u32 fcmd = (u32)(e)->args[1]; \
				u32 fsz = 0; \
				if (fcmd == 15 || fcmd == 16 || fcmd == 1035 || fcmd == 1036 || fcmd == 1037 || fcmd == 1038 || fcmd == 1039 || fcmd == 1040 || fcmd == 1043 || fcmd == 1044 || fcmd == 19 || fcmd == 20 || fcmd == 21 || fcmd == 22 || fcmd == 23 || fcmd == 24) fsz = 8; \
				else if (fcmd == 5 || fcmd == 6 || fcmd == 7 || fcmd == 12 || fcmd == 13 || fcmd == 14 || fcmd == 36 || fcmd == 37 || fcmd == 38) fsz = 32; \
				long __err = (fsz > 0 && (e)->args[2]) ? bpf_probe_read_user((e)->str_arg + 1024, fsz, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && fsz > 0 && (e)->args[2]) ? fsz : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 79: /* getcwd */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0), (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 89: /* readlink */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 96: /* gettimeofday */ \
			(e)->ptr = (e)->args[0]; \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1040, 8, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1040 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 97: /* getrlimit */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 99: /* sysinfo */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, 112, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 112 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 125: /* capget */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 24, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 24 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 137: /* statfs */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 120, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 120 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 138: /* fstatfs */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 120, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 120 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 157: /* prctl */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 158: /* arch_prctl */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 159: /* adjtimex */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, 208, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 208 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 191: /* getxattr */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 192: /* lgetxattr */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 768, ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 768 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 193: /* fgetxattr */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 256, ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->args[3] > 0 ? ((e)->args[3] > 256 ? 256 : (e)->args[3]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 256 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 194: /* listxattr */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 512, ((e)->args[2] > 0 ? ((e)->args[2] > 256 ? 256 : (e)->args[2]) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] > 256 ? 256 : (e)->args[2]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 195: /* llistxattr */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 512, ((e)->args[2] > 0 ? ((e)->args[2] > 256 ? 256 : (e)->args[2]) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] > 256 ? 256 : (e)->args[2]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 512 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 196: /* flistxattr */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg, ((e)->args[2] > 0 ? ((e)->args[2] > 256 ? 256 : (e)->args[2]) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->args[2] > 0 ? ((e)->args[2] > 256 ? 256 : (e)->args[2]) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 0 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 206: /* io_setup */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 208: /* io_getevents */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret * 32 > 512 ? 512 : (e)->ret * 32) : 0), (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? ((e)->ret > 0 ? ((e)->ret * 32 > 512 ? 512 : (e)->ret * 32) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 228: /* clock_gettime */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 229: /* clock_getres */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 230: /* clock_nanosleep */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 232: /* epoll_wait */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret * 12 > 512 ? 512 : (e)->ret * 12) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->ret > 0 ? ((e)->ret * 12 > 512 ? 512 : (e)->ret * 12) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 247: /* waitid */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, 128, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 128 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[4] ? bpf_probe_read_user((e)->str_arg + 1160, 144, (void *)(e)->args[4]) : 0; \
				long pr = (__err == 0 && (e)->args[4]) ? 144 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 4)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1160 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 262: /* newfstatat */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, 144, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 144 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 267: /* readlinkat */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0), (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? ((e)->ret > 0 ? ((e)->ret > 512 ? 512 : (e)->ret) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 271: /* ppoll */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0), (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? ((e)->args[1] > 0 ? ((e)->args[1] * 8 > 512 ? 512 : (e)->args[1] * 8) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 274: /* get_robust_list */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1040, 8, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1040 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 281: /* epoll_pwait */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret * 12 > 512 ? 512 : (e)->ret * 12) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->ret > 0 ? ((e)->ret * 12 > 512 ? 512 : (e)->ret * 12) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 288: /* accept4 */ \
			(e)->ptr = (e)->args[1]; \
			{ \
				u32 addrlen = 0; \
				bpf_probe_read_user(&addrlen, 4, (void *)(e)->args[2]); \
				u32 inlen = *(u32 *)((e)->str_arg + 768); \
				if (inlen > 0 && inlen < addrlen) addrlen = inlen; \
				addrlen = (addrlen > 128) ? 128 : addrlen; \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, addrlen, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? addrlen : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 772, 4, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 4 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 772 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 293: /* pipe2 */ \
			{ \
				long __err = (e)->args[0] ? bpf_probe_read_user((e)->str_arg + 1024, 8, (void *)(e)->args[0]) : 0; \
				long pr = (__err == 0 && (e)->args[0]) ? 8 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 0)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 302: /* prlimit64 */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 1024, 16, (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? 16 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 305: /* clock_adjtime */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, 208, (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? 208 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 333: /* io_pgetevents */ \
			{ \
				long __err = (e)->args[3] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret * 32 > 512 ? 512 : (e)->ret * 32) : 0), (void *)(e)->args[3]) : 0; \
				long pr = (__err == 0 && (e)->args[3]) ? ((e)->ret > 0 ? ((e)->ret * 32 > 512 ? 512 : (e)->ret * 32) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 3)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 441: /* epoll_pwait2 */ \
			{ \
				long __err = (e)->args[1] ? bpf_probe_read_user((e)->str_arg + 1024, ((e)->ret > 0 ? ((e)->ret * 12 > 512 ? 512 : (e)->ret * 12) : 0), (void *)(e)->args[1]) : 0; \
				long pr = (__err == 0 && (e)->args[1]) ? ((e)->ret > 0 ? ((e)->ret * 12 > 512 ? 512 : (e)->ret * 12) : 0) : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 1)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
		case 451: /* cachestat */ \
			{ \
				long __err = (e)->args[2] ? bpf_probe_read_user((e)->str_arg + 1024, 40, (void *)(e)->args[2]) : 0; \
				long pr = (__err == 0 && (e)->args[2]) ? 40 : __err; \
				if (pr < 0) { \
					s32 curr = (e)->probe_ret_exit; \
					u32 mask = (curr < -1) ? (u32)(-curr - 1) : 0; \
					(e)->probe_ret_exit = -(s32)((mask | (1 << 2)) + 1); \
				} else { \
					if ((e)->probe_ret_exit == -1) (e)->probe_ret_exit = 0; \
					u32 req_len = 1024 + pr; \
					if ((e)->data_len < req_len) (e)->data_len = req_len; \
				} \
			} \
			break; \
	}
#endif
