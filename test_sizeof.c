#include <stdio.h>
int main() {
    long proto_cbvs2[] = {
        (long) 0, (long) 0, (long) 0,
        (long) 0, (long) 0,
        (long) 0, (long) 0,
    };
    printf("%zu\n", sizeof(proto_cbvs2));
    return 0;
}
